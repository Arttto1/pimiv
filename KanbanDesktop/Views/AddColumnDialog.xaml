<Window x:Class="KanbanDesktop.Views.AddColumnDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Nova Coluna"
        Height="350" Width="400"
        Background="{StaticResource BackgroundBlack}"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Window.Resources>
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="{StaticResource CardBlack}"/>
            <Setter Property="Foreground" Value="{StaticResource TextWhite}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderGray}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton" 
                                        Grid.Column="2" 
                                        Focusable="false"
                                        IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                        ClickMode="Press"
                                        Background="{StaticResource CardBlack}"
                                        BorderBrush="{StaticResource BorderGray}"
                                        BorderThickness="1">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border x:Name="Border" 
                                              Background="{TemplateBinding Background}"
                                              BorderBrush="{TemplateBinding BorderBrush}"
                                              BorderThickness="{TemplateBinding BorderThickness}">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition/>
                                                    <ColumnDefinition Width="20"/>
                                                </Grid.ColumnDefinitions>
                                                <ContentPresenter Grid.Column="0" 
                                                                Margin="10,5"
                                                                Content="{TemplateBinding Content}"
                                                                VerticalAlignment="Center"
                                                                HorizontalAlignment="Left"/>
                                                <Path x:Name="Arrow"
                                                    Grid.Column="1"
                                                    Fill="{StaticResource TextWhite}"
                                                    HorizontalAlignment="Center"
                                                    VerticalAlignment="Center"
                                                    Data="M 0 0 L 4 4 L 8 0 Z"/>
                                            </Grid>
                                        </Border>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter x:Name="ContentSite"
                                            IsHitTestVisible="False"
                                            Content="{TemplateBinding SelectionBoxItem}"
                                            ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                            ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                            Margin="10,5"
                                            VerticalAlignment="Center"
                                            HorizontalAlignment="Left"/>
                            <Popup x:Name="Popup"
                                 Placement="Bottom"
                                 IsOpen="{TemplateBinding IsDropDownOpen}"
                                 AllowsTransparency="True"
                                 Focusable="False"
                                 PopupAnimation="Slide">
                                <Grid x:Name="DropDown"
                                    SnapsToDevicePixels="True"
                                    MinWidth="{TemplateBinding ActualWidth}"
                                    MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border x:Name="DropDownBorder"
                                          Background="{StaticResource CardBlack}"
                                          BorderBrush="{StaticResource BorderGray}"
                                          BorderThickness="1">
                                        <ScrollViewer Margin="0" SnapsToDevicePixels="True">
                                            <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"/>
                                        </ScrollViewer>
                                    </Border>
                                </Grid>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="ComboBoxItem">
            <Setter Property="Background" Value="{StaticResource CardBlack}"/>
            <Setter Property="Foreground" Value="{StaticResource TextWhite}"/>
            <Setter Property="Padding" Value="10,5"/>
            <Style.Triggers>
                <Trigger Property="IsHighlighted" Value="True">
                    <Setter Property="Background" Value="{StaticResource BorderGray}"/>
                    <Setter Property="Foreground" Value="{StaticResource TextWhite}"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{StaticResource DarkGreen}"/>
                    <Setter Property="Foreground" Value="{StaticResource TextWhite}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <Grid Margin="20">
        <StackPanel>
            <TextBlock Text="Nome da Coluna" 
                       Foreground="{StaticResource TextWhite}" 
                       Margin="0,0,0,5"/>
            <TextBox x:Name="NameTextBox" 
                     Height="35" 
                     FontSize="14"
                     Background="{StaticResource CardBlack}"
                     Foreground="{StaticResource TextWhite}"
                     BorderBrush="{StaticResource BorderGray}"
                     BorderThickness="1"
                     Padding="10,5"/>

            <TextBlock Text="Cor da Coluna" 
                       Foreground="{StaticResource TextWhite}" 
                       Margin="0,20,0,5"/>
            <ComboBox x:Name="ColorComboBox" 
                      Height="35" 
                      FontSize="14"
                      Background="{StaticResource CardBlack}"
                      Foreground="{StaticResource TextWhite}"
                      BorderBrush="{StaticResource BorderGray}"
                      BorderThickness="1"
                      SelectedIndex="0">
                <ComboBoxItem Content="Vermelho" Tag="red"/>
                <ComboBoxItem Content="Verde" Tag="green"/>
                <ComboBoxItem Content="Azul" Tag="blue"/>
                <ComboBoxItem Content="Amarelo" Tag="yellow"/>
                <ComboBoxItem Content="Laranja" Tag="orange"/>
                <ComboBoxItem Content="Rosa" Tag="pink"/>
                <ComboBoxItem Content="Marrom" Tag="brown"/>
                <ComboBoxItem Content="Preto" Tag="black"/>
                <ComboBoxItem Content="Branco" Tag="white"/>
                <ComboBoxItem Content="Cinza" Tag="gray"/>
            </ComboBox>

            <StackPanel Orientation="Horizontal" 
                        HorizontalAlignment="Right" 
                        Margin="0,30,0,0">
                <Button Content="CANCELAR" 
                        Width="100" 
                        Height="35"
                        Background="{StaticResource BorderGray}"
                        Foreground="{StaticResource TextWhite}"
                        BorderThickness="0"
                        Margin="0,0,10,0"
                        Cursor="Hand"
                        Click="Cancel_Click"/>
                <Button Content="CRIAR" 
                        Width="100" 
                        Height="35"
                        Background="{StaticResource DarkGreen}"
                        Foreground="{StaticResource TextWhite}"
                        BorderThickness="0"
                        Cursor="Hand"
                        Click="Create_Click"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
